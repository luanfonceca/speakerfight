sudo: false
language:
  - python
install:
  - pip install -r requirements.txt
script:
  - coverage run manage.py test
after_success:
  - coveralls
  - if [ "$TRAVIS_BRANCH" = "master" ]; then fab deploy; fi
