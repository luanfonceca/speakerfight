dependencies:
  override:
    - pip install -r requirements.txt
test:
  override:
    - coverage run manage.py test
    - coveralls
deployment:
  production:
    branch: test-circle
    commands:
      - fab deploy
